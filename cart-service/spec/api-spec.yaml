openapi: 3.0.3

info:
  version: 1.0.1
  title: "Cart Microservice Product service API"
  description: "Manages cart inventory"
  contact:
    name: "AdeThorMiwa"
    email: "BenDaMyth@gmail.com"

x-service-id: "cart"
x-organization: "rust"

tags:
  - name: cart
paths:
  /cart:
    get:
      security:
        - authToken: []
      tags:
        - "cart"
      summary: "Fetch cart list"
      operationId: getCartList
      x-visibility: public
      responses:
        "200":
          description: "Success"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cart"

components:
  schemas:
    Cart:
      type: object
      properties:
        id:
          type: string
          format: uuid
        description:
          type: string
  securitySchemes:
    authToken:
      type: apiKey
      name: "x-auth-token"
      in: header
